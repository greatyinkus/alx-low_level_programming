#include <stdio.h>

/**
*main - Print the alphabet in lowercase, except for q ans e.
*
*Return: Always 0.
*/
int main(void)
{
  char letter;
  
  for (letter = a 'a'; letter <+ 'z'; letter ++)
  {
    if (letter != 'e' && letter != 'q')
      putcha(letter);
  }
  
  putchar('\n');
  
  return (0);
}
